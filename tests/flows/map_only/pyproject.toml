[project]
name = "map-only-flow"
version = "0.1.0"
description = "Test pipeline for map without aggregation"
requires-python = ">=3.13"
dependencies = ["lokkiflow @ file:///${PROJECT_ROOT}/../../../"]
