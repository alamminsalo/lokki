[project]
name = "docker-build-batch-test"
version = "0.1.0"
description = "Test flow with batch step for Docker build testing"
requires-python = ">=3.13"
dependencies = [
  "lokkiflow @ file:///${PROJECT_ROOT}/../../../",
]
